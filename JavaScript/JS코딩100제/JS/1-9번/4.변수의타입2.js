//다음 변수 a를 `typeof(a)`로 넣었을 때 출력될 값과의 연결이 알맞지 않은 것은?

//1)  입력 : a =1,   출력 : number

//2)  입력 : a = 2.22,   출력 : boolean
//  - 2.22는 숫자 타입인 number를 써주어야한다. JS에서는 number의 종류는 실수 1가지 밖에 없다.

//3)  입력 : a = 'p',   출력 : string

//4)  입력 : a = [1, 2, 3],   출력 : object
//  - 배열은 원시 타입이 아니기 때문에 객체에 속한다.
//  - JS는 원시 타입이 아닌 타입은 모두 object 타입이다.

// 정답: 2번
